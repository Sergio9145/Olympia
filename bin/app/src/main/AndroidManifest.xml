<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.olympia">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.olympia.SampleApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.olympia.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".WordsListActivity"
            android:parentActivityName=".MainActivity">
        </activity>

        <activity android:name=".WordCardActivity"
            android:parentActivityName=".WordsListActivity">
        </activity>

        <activity android:name=".SettingsActivity"
            android:parentActivityName=".WordsListActivity">
        </activity>

        <activity android:name=".LegalActivity"
            android:parentActivityName=".WordsListActivity">
        </activity>

        <activity android:name=".AboutActivity"
            android:parentActivityName=".WordsListActivity">
        </activity>

    </application>
</manifest>